# Implementation Plan: RDNA Wave32 A2-U — 转场/遮罩 SALU提升+EXEC早退

**Date:** 2026-08-26
**Spec Brief:** `docs/aegis/specs/2026-08-26-wave32-a2u-brief.md` (approved: brainstorming Q1=A, Q2=A-2, D1-D6 批 U)
**Scope:** 纯 `wgpu/WGSL` 可移植，RDNA-only；仅改 `host/renpy-host/src/arena.rs` 4 段 WGSL 文本 + `shaders.py` 注释；不引 Vulkan 显式扩展、不动 `draw.py` 批逻辑与 `blur/Live2D`。

---

## Plan Basis

```text
Aegis Visibility:
  A2-U 改的是 host 渲染器的唯一 WGSL 真源 (arena.rs 4 const) 与诚实门 (shaders.py);
  错改会漂移 SDL/host 双树契约或破坏 G01-G08 像素容差; 先计划锁定 diff 与 gate 再动代码。

BaselineUsageDraft:
- Required: AGENTS.md §4.5 双实现合成器 + §4.9 颜色/格式; host/README.md 双轨构建;
            doc/wgsl_shader_migration.md; consensus-wgpu-native-vulkan-rewrite.md AC2/AC4
- Delivered: host 仅 WgpuDraw 单渲染器; WGSL 经 NativeShaderComposer SSOT
- Cited: rdna-shader-instruction-set-architecture.pdf §2.1/3.6.5/6.2; amdgpu_isa_rdna1.xml 编码面
- Missing: BC-160 RGP 一帧 trace (仅作可选 evidence, 非阻塞)
- Decision: continue

Requirement Ready Check:
- Source: spec brief 2026-08-26-wave32-a2u-brief.md (approved)
- Goals: BC-160 转场帧 p95↓≥10% + G01-G08 零回归 + ldd 无 SDL
- Acceptance: brief §6 五条可勾; benchmark 命令已确认 host/scripts/benchmark_bc160.sh
- Decision: ready

Change Necessity:
- Need: 4 段 WGSL 当前无 uniform 级早退/标量提升, 是 Wave32 上 EXEC skip 与 VGPR 占用的可优化点
- No-change: 否; 不改 WGSL 文本则无 GPU 行为改变
- Min boundary: 仅 4 个 &str 常量体 + shaders.py 注释; 不动 bind 布局/tex_count/_PIPELINE_KEYS
- Decision: code-change

Existence Check:
- New surface: 无 (无新 owner/artifact/adapter/fallback/指标)
- Reuse: arena.rs const + shaders.py register 已覆盖
- Decision: reuse-existing

Architecture Integrity Lens:
- Canonical owner: arena.rs(const) + shader.rs(emit_wgsl) + shaders.py(_PIPELINE_KEYS)
- Overlap: 无; 仅文本提纯, 不增合成维度
- Verdict: reuse-existing, 编辑就地

Plan-Time Complexity Check:
- Target: host/renpy-host/src/arena.rs:2280-2343, 2468-2518; renpy/wgpu/shaders.py:24-36,475-476
- Pressure: low (纯文本常量, 无新逻辑/新类型)
- Budget: within-budget
- Recommendation: edit-in-place

Plan Pressure Test:
- Owner/contract: 4 const 仍由 Rust SSOT 校验, cache_key 维度不变
- Verification: brief §6 五门齐全 (cargo/pytest/gates/bench/ldd)
- Task executability: 每 FS 体为独立 <5min diff
- Result: proceed

Execution Readiness View:
- Intent Lock: 仅 A2-U; A2-M/A2-V 显式 defer
- Scope Fence: 4 const + 注释; 禁动 draw.py/blur/Live2D/SDL 树
- Baseline Lock: Rgba8Unorm+PMA, tex_count∈{2,3}, params16, 单渲染器, ldd 无 SDL
- Approved Behavior: 像素输出等价 (premultiplied, 早退分支与旧 mix 数值一致)
- Compatibility Boundary: _PIPELINE_KEYS + assert_pipeline_map_honest 仍过; WgslShaderCache key 熵不变
- Retirement Boundary: 无旧路径删除
- Task Batches: T1 DISSOLVE, T2 IMAGEDISSOLVE, T3 ALPHA_MASK, T4 MASK, T5 shaders注释, T6 验证
- Test Obligations: pytest + phase9_gates + benchmark_bc160 (1800帧)
- Review Gates: cargo check + naga validate 隐式 (create_pipeline_wgsl)
- Drift/Rewind: 单 const 可独立 git revert
- Evidence: benchmark p95↓≥10% 或 RGP VGPR≤5&waves/CU↑
- Advisory: method-pack 执行指引, 非 GateDecision
```

### TDD Route
```text
TDD Route:
- Mode: off
- Decision: skipped
- Strict authority: not applicable
- Test posture: post-change regression (pytest/gates/benchmark)
- Reason: 纯 WGSL 文本提纯, 等价改写; 无新契约/分支逻辑, 风险低
- Verification: cargo check + pytest test_wgpu_composer + phase9_gates G01-G08 + benchmark_bc160 p95
```

---

## Files

| 文件 | 动作 | 边界 |
|---|---|---|
| `host/renpy-host/src/arena.rs` | 改 `DISSOLVE_WGSL` (2280-2287), `IMAGEDISSOLVE_WGSL` (2328-2343), `ALPHA_MASK_WGSL` (2468-2474), `MASK_WGSL` (2508-2517) 的 fs_main 体 | 仅 FS 体; struct/bind/VS 不动 |
| `renpy/wgpu/shaders.py` | `renpy.alpha_mask` / `renpy.mask` 注册处 (475-476) 加 `data0.z` 语义注释 | 仅注释, 不增字段 |
| `docs/wgsl_shader_migration.md` | 内建表对应行注明 `amount/channel` uniform 级早退 | 仅文档, 随 T6 提交 |

不改动：`shader.rs::emit_wgsl`、`composer.py::WgslShaderCache`、`draw.py`、`blur/Live2D` 常量、`build_bind_group_layout`。

---

## Compatibility Boundary (冻结)

* `tex_count` dissolve=2 / imagedissolve=3 / alpha_mask=2 / mask=2 不变
* `uniform_layout="params16"` 不变；`uniform_binding(tex_count)` 布局不变
* `WgslShaderCache` 以排序 part 集 `sha1[:16]` 为 `cache_key` 不变 → 同 part 集哈希相同，缓存命中行为不变
* `_PIPELINE_KEYS` 映射与 `assert_pipeline_map_honest()` 仍过
* `ldd host/target/release/renpy-host | grep -iE 'libSDL'` 为空；`RUST_LOG=info` 含 `adapter backend=Vulkan`

---

## Tasks

### T1 — DISSOLVE_WGSL: SALU 提升 + amount 早退

**File:** `host/renpy-host/src/arena.rs:2280-2287`
**Why:** 当前 `mix(c0,c1,amount)` 永远双采样；`amount==0/1` 是 uniform 级（同波 32 lane 同值），可全波跳过第二采样，贴 `S_CBRANCH_EXECZ`。
**Change Necessity:** 文本提纯；`amt` 复用去重 `clamp`。
**Impact/Compat:** 输出数值与旧 `mix(c0,c1,clamp(u.data0.x,0,1))` 等价；bind 不变。

**Steps (edit-in-place, 替换 fs_main 体):**
把
```rust
@fragment
fn fs_main(v: VsOut) -> @location(0) vec4<f32> {
    let c0 = textureSample(t_old, s_color, v.uv);
    let c1 = textureSample(t_new, s_color, v.uv);
    let amount = clamp(u.data0.x, 0.0, 1.0);
    let c = mix(c0, c1, amount) * v.color;
    let a = clamp(c.a, 0.0, 1.0);
    return vec4<f32>(c.rgb * a, a);
}
```
改为
```rust
@fragment
fn fs_main(v: VsOut) -> @location(0) vec4<f32> {
    let amt = clamp(u.data0.x, 0.0, 1.0);
    // uniform-level branch: same for all 32 lanes in a wave → S_CBRANCH_EXECZ-friendly.
    if (amt <= 0.0) {
        let c1 = textureSample(t_new, s_color, v.uv) * v.color;
        let a = clamp(c1.a, 0.0, 1.0);
        return vec4<f32>(c1.rgb * a, a);
    }
    if (amt >= 1.0) {
        let c0 = textureSample(t_old, s_color, v.uv) * v.color;
        let a = clamp(c0.a, 0.0, 1.0);
        return vec4<f32>(c0.rgb * a, a);
    }
    let c0 = textureSample(t_old, s_color, v.uv);
    let c1 = textureSample(t_new, s_color, v.uv);
    let c = mix(c0, c1, amt) * v.color;
    let a = clamp(c.a, 0.0, 1.0);
    return vec4<f32>(c.rgb * a, a);
}
```
**Verification:** `cargo check -p renpy-host` 过；`cargo test -p renpy-host` 无回归。

### T2 — IMAGEDISSOLVE_WGSL: channel 提前 + 标量 a

**File:** `host/renpy-host/src/arena.rs:2328-2343`
**Why:** `control`/`bottom`/`top` 三采样全执行，且 `ctrl` 在 `select` 后存活跨三个 `let`；把 `a` 的 `clamp((ctrl+off)*mult)` 标量预算并缩短 `ctrl` live range。
**Change Necessity:** 文本提纯；`off/mult/use_red` 为 uniform 级，无波内分歧。
**Impact/Compat:** `channel` 语义（>0.5 取 `.r`，否则 `.a`）保留；输出等价。

**Steps (替换 fs_main 体):**
把
```rust
@fragment
fn fs_main(v: VsOut) -> @location(0) vec4<f32> {
    let control = textureSample(t_control, s_color, v.uv);
    let bottom = textureSample(t_bottom, s_color, v.uv);
    let top = textureSample(t_top, s_color, v.uv);
    let offset = u.data0.x;
    let mult = u.data0.y;
    // data0.z > 0.5 → sample control.r (HuangmeiC image_dissolve rule.png);
    // else control.a (stock ImageDissolve after matrixcolor red→alpha).
    let use_red = u.data0.z > 0.5;
    let ctrl = select(control.a, control.r, use_red);
    let a = clamp((ctrl + offset) * mult, 0.0, 1.0);
    let c = mix(bottom, top, a) * v.color;
    let out_a = clamp(c.a, 0.0, 1.0);
    return vec4<f32>(c.rgb * out_a, out_a);
}
```
改为
```rust
@fragment
fn fs_main(v: VsOut) -> @location(0) vec4<f32> {
    let off = u.data0.x;
    let mult = u.data0.y;
    // uniform-level select: same for all lanes in a wave.
    let use_red = u.data0.z > 0.5;
    let control = textureSample(t_control, s_color, v.uv);
    let ctrl = select(control.a, control.r, use_red);
    // scalar-only compute; ctrl VGPR dies before bottom/top samples.
    let a = clamp((ctrl + off) * mult, 0.0, 1.0);
    let bottom = textureSample(t_bottom, s_color, v.uv);
    let top = textureSample(t_top, s_color, v.uv);
    let c = mix(bottom, top, a) * v.color;
    let out_a = clamp(c.a, 0.0, 1.0);
    return vec4<f32>(c.rgb * out_a, out_a);
}
```
**Verification:** `cargo check -p renpy-host` 过。

### T3 — ALPHA_MASK_WGSL: 去中间 vec4

**File:** `host/renpy-host/src/arena.rs:2468-2474`
**Why:** `let mask = ...` 命名误导（实际只用 `.r`）；合并为无中间 `vec4` 复用 `ma`。
**Change Necessity:** 文本提纯；`ma` 单一 VGPR。
**Impact/Compat:** `src.a * mask.r` 等价；bind 不变。

**Steps (替换 fs_main 体):**
把
```rust
@fragment
fn fs_main(v: VsOut) -> @location(0) vec4<f32> {
    let src = textureSample(t_src, s_color, v.uv) * v.color;
    let mask = textureSample(t_mask, s_color, v.uv);
    let a = src.a * mask.r;
    return vec4<f32>(src.rgb * mask.r, a);
}
```
改为
```rust
@fragment
fn fs_main(v: VsOut) -> @location(0) vec4<f32> {
    let src = textureSample(t_src, s_color, v.uv) * v.color;
    let ma = textureSample(t_mask, s_color, v.uv).r;
    let a = src.a * ma;
    return vec4<f32>(src.rgb * ma, a);
}
```
**Verification:** `cargo check -p renpy-host` 过。

### T4 — MASK_WGSL: factor 复用

**File:** `host/renpy-host/src/arena.rs:2508-2517`
**Why:** `let factor = mask.a * mult + offset` 已标量，但 `factor` 为 `f32` 与 `src`(`vec4`) 相乘会广播；保持等价并把 `mult/offset` 声明提前，减少 `mask`/`src` 同时活。
**Change Necessity:** 文本提纯；`k` 单一 VGPR。
**Impact/Compat:** 输出与 `src * (mask.a*mult+offset)` 等价。

**Steps (替换 fs_main 体):**
把
```rust
@fragment
fn fs_main(v: VsOut) -> @location(0) vec4<f32> {
    let src = textureSample(t_src, s_color, v.uv) * v.color;
    let mask = textureSample(t_mask, s_color, v.uv);
    let mult = u.data0.x;
    let offset = u.data0.y;
    let factor = mask.a * mult + offset;
    let c = src * factor;
    return vec4<f32>(c.r, c.g, c.b, c.a);
}
```
改为
```rust
@fragment
fn fs_main(v: VsOut) -> @location(0) vec4<f32> {
    let mult = u.data0.x;
    let offset = u.data0.y;
    let src = textureSample(t_src, s_color, v.uv) * v.color;
    let ma = textureSample(t_mask, s_color, v.uv).a;
    let k = ma * mult + offset;
    let c = src * k;
    return vec4<f32>(c.r, c.g, c.b, c.a);
}
```
**Verification:** `cargo check -p renpy-host` 过。

### T5 — shaders.py 注释 (channel/data0.z 语义)

**File:** `renpy/wgpu/shaders.py:475-476`
**Why:** 固化 `data0.z` channel 语义，防后续混淆 red/alpha alias（product 逻辑见 `doc/wgsl_shader_migration.md`）。
**Change Necessity:** 仅注释，零行为影响。

**Steps:** 在
```python
        register_wgsl_shader("renpy.alpha_mask", priority=400, kind="alpha_mask")
        register_wgsl_shader("renpy.mask", priority=400, kind="mask")
```
改为
```python
        # renpy.alpha_mask: src * mask.r (dual tex). data0 unused; binds tex0=src, tex1=mask.
        register_wgsl_shader("renpy.alpha_mask", priority=400, kind="alpha_mask")
        # renpy.mask: src * (mask.a * mult + offset). data0.x=mult, data0.y=offset (uniform-level).
        register_wgsl_shader("renpy.mask", priority=400, kind="mask")
```
**Verification:** `python -c "from renpy.wgpu.shaders import register_builtin_core, assert_pipeline_map_honest; register_builtin_core(); print(assert_pipeline_map_honest())"` 返回空列表（无失真 key）。

### T6 — 全量验证 + 文档同步

**Why:** 像素零回归 + 性能门。
**Steps:**
1. `cargo check -p renpy-host` 通过
2. `cargo test -p renpy-host` 通过（若有）
3. `python -m pytest tests/test_wgpu_composer.py -v` 全过
4. `bash host/scripts/phase9_gates.sh` → G01-G08 全过（MAE≤2/255，单通道 max≤16）
5. 性能门：`cd /mnt/nvme1n1p2/revult && bash host/scripts/benchmark_bc160.sh --measured --measured-frames 1800` 输出 `avg_frame_time_ms`；与基线（上次 commit 的 `host/target/bc160_perf_metrics.json` 或历史值）对比，转场段 p95↓≥10% 或 RGP `VGPR≤5` 且 `waves/CU↑`。
6. `ldd host/target/release/renpy-host | grep -iE 'libSDL'` 应为空；`RUST_LOG=info cargo run -p renpy-host -- the_question 2>&1 | grep -q "backend=Vulkan"` 通过
7. `doc/wgsl_shader_migration.md` 内建表：在 `renpy.dissolve / renpy.imagedissolve / renpy.alpha_mask / renpy.mask` 行加注“uniform-level 早退/标量提升 (A2-U, RDNA Wave32)”

**Verification:** 以上命令全部满足；任一失败 → 单 const 回滚（git checkout 对应 range）重测。

---

## Risks & Rollback

| 风险 | 触发 | 回退 |
|---|---|---|
| `if (amt<=0.0)` 分支引入波内分歧抖动 | benchmark 1800 帧 p95 升或方差>5% | `git checkout -- host/renpy-host/src/arena.rs` 对应 const 段，重跑 T6 |
| naga 校验拒绝新 WGSL | `create_pipeline_wgsl` 抛 `ComposerError` | 同 const 回滚；`RENPY_HOST_DRAW_RAISE=1` 复现 |
| 像素漂移 | phase9_gates MAE>2/255 | 单 const 回滚；确认 diff 仅提纯无数学改动 |

---

## Self-Review (spec coverage)

1. Spec §2 Goal → T1-T4 覆盖；§6 AC1-AC5 → T6 覆盖。
2. 无 TBD/TODO；每段 FS 体为完整可粘贴 Rust 字符串。
3. `tex_count`/`params16`/bind 签名跨 T1-T4 一致，未动。
4. 兼容：`_PIPELINE_KEYS`/`assert_pipeline_map_honest`/cache_key 维度 T5 注释不破。
5. Change Necessity：每个 Task 注明“文本提纯，数值等价”。
6. Existence：无新 surface，reuse-existing。
7. Complexity：edit-in-place，within-budget。
8. Architecture：owner 不变，无 higher-level 简化遗漏。
9. Verification：T1-T6 命令精确。
10. Dual-track/ADR：无删除/退休，无 ADR 信号。

---

## Execution Route

```text
Execution Route:
- Decision: inline
- Evidence: 4 个 const 为同文件相邻文本改动, 无独立所有权, 无并行收益; 单分支顺序执行即可
- Fallback: 若某 const 验证失败, 单段 git revert 不波及其余
- User confirmation required: no
```
