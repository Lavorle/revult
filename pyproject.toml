[project]
name = "renpy"
readme = "README.rst"
license = "MIT"
version= "8.99.99"
requires-python = ">=3.12.8,<3.13"
dependencies = [
    "future",
    "six",
    "pefile",
    "requests",
    "rsa",
    "setuptools",
    "cython",
    "ruff",
    "sphinx",
    "sphinx_rtd_theme",
    "sphinx_rtd_dark_mode",
    "sphinx-tabs",
    "jinja2",
    "pkgconfig>=1.6.0",
    "chardet>=7.4.3",
    "websockets>=16.0",
    "pysocks>=1.7.1",
]


# For now those are listed in project requirements until build system is updated.
# [project.optional-dependencies]
# dev = ["setuptools", "cython", "ruff"]

# For now, Ren'Py isn't using a build system, as build isolation makes building too slow.

# [build-system]
# requires = ["setuptools", "cython"]
# build-backend = "setuptools.build_meta:__legacy__"

[tool.pyright]
include = [
    "*.py",
    "renpy/",
    "src/"
]

exclude = [
    "module/build/",
]

ignore = [
    "renpycoverage.py"
]

typeCheckingMode = "basic"

reportOptionalSubscript = false
reportOptionalMemberAccess = false
reportOptionalCall = false
reportOptionalIterable = false
reportOptionalContextManager = false
reportOptionalOperand = false
reportMissingModuleSource = false
reportUnnecessaryTypeIgnoreComment = "information"
reportTypeCommentUsage = "information"

[tool.cyright]
include = [
    "*.py",
    "renpy/",
    "src/"
]

exclude = [
    "module/build/",
]

ignore = [
    "renpycoverage.py"
]

reportGeneralTypeIssues = false
reportOptionalSubscript = false
reportOptionalMemberAccess = false
reportOptionalCall = false
reportOptionalIterable = false
reportOptionalContextManager = false
reportOptionalOperand = false
reportMissingModuleSource = false



[tool.ruff]
line-length = 120
extend-exclude = [
    "keywords.py",
    "renpy/text/emoji_trie.py",
]

[tool.ruff.lint.per-file-ignores]
# Host shims intentionally swallow broad exceptions and use fallback patterns
# to keep Ren'Py parity (do not crash on fallback). Bulk noqa was 4994->0 narrow;
# now explicit per-file-ignores. Remaining 11 codes below are intentional fallbacks
# (generic Exception mirrors Ren'Py API, try-continue skips bad data, useless-if
# guards keep GL2-compat, SRCALPHA probe via NameError, renpy global optional).
"host/python/**/*.py" = ["BLE001", "S110", "S112", "TRY002", "TRY004", "B018", "F821", "F811", "RUF034", "SIM102"]

[tool.ruff.format]
